<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ExamenHockey" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/azulPrimario</item>
        <item name="colorPrimaryVariant">@color/azulOscuro</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="Theme.ExamenHockey" parent="Base.Theme.ExamenHockey" />
    
    <style name="Theme.ExamenHockey.NoActionBar" parent="Theme.ExamenHockey">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>